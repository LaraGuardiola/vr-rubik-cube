node_modules/
*.log
screenshot*.png
.vite/
.DS_Store
# dist/ is a legacy build location; the current build outputs to docs/ which
# IS committed (GitHub Pages deploys it directly from the main branch).
dist/
